a
```markdown
| Figure | Layers |
| --- | --- |
| a | 6 layers, 7 layers, 10 layers |
| b | 4 layers, 5 layers, 8 layers |
| c | 2 layers, 3 layers, 6 layers |
```
b
```markdown
| Figure | Layers |
| --- | --- |
| a | 6 layers, 7 layers, 10 layers |
| b | 4 layers, 5 layers, 8 layers |
| c | 2 layers, 3 layers, 6 layers |
```
c
```markdown
| Figure | Layers |
| --- | --- |
| a | 6 layers, 7 layers, 10 layers |
| b | 4 layers, 5 layers, 8 layers |
| c | 2 layers, 3 layers, 6 layers |
```
