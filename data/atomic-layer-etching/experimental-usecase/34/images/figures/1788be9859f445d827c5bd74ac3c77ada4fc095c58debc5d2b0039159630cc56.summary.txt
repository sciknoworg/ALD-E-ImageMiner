The chart displays the relationship between binding energy (in eV) and counts per second (counts/s). It includes a raw data series, a fitted curve, and three decomposed components labeled AIF, AIIOF, and AIO.